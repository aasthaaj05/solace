import React from "react";

const AnimatedBackground = () => {
  return (
    <div className="fixed inset-0 z-[-1] pointer-events-none bg-transparent"></div>
  );
};

export default AnimatedBackground;

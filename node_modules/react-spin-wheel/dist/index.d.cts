import * as react_jsx_runtime from 'react/jsx-runtime';
import React from 'react';

type Color = string;
type ColorArray = Color[];

// each item should have a unique name
type SpinWheelItem = {
  name: string
}
type SpinWheelProps = {
  itemColors?: ColorArray,
  borderColor?: Color,
  items?: SpinWheelItem[] | string[],
  spinActionName?: string,
  resetActionName?: string,
  size?: number,
  spinTime?: number,
  onResult?: (result?:SpinWheelItem | string) => void,
  onFinishSpin?: (result?:SpinWheelItem | string) => void,
  onReset?: () => void,
  spinContainerStyle?: React.CSSProperties,
  spinWheelStyle?: React.CSSProperties,
  spinButtonStyle?: React.CSSProperties,
  resetButtonStyle?: React.CSSProperties,
  spinItemStyle?: React.CSSProperties,
  spinFontStyle?: React.CSSProperties
}

declare const SpinWheel: ({ itemColors, borderColor, items, spinActionName, resetActionName, size, spinTime, onResult, onFinishSpin, onReset, spinContainerStyle, spinWheelStyle, spinButtonStyle, resetButtonStyle, spinFontStyle, spinItemStyle, }: SpinWheelProps) => react_jsx_runtime.JSX.Element;

export { SpinWheel };
